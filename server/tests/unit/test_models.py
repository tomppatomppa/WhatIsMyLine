"""
This file (test_models.py) contains the unit tests for the models.py file.
"""
from project.models import User


def test_new_user():
    pass
    # """
    # GIVEN a User model
    # WHEN a new User is created
    # THEN check the user_id, email, password_hashed, provider, authenticated, and active fields are defined correctly
    # """
    # user = User("1234", 'kalle@gmail.com', 'secretPassword', "google")
    # assert user.user_id == '1234'
    # assert user.email == 'kalle@gmail.com'
    # assert user.password_hashed != 'secretPassword'
    # assert user.provider == 'google'
    # assert user.__repr__() == '<User: kalle@gmail.com>'
    # assert user.is_authenticated
    # assert user.is_active
    # assert not user.is_anonymous

